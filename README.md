## **Assignment 1**
1. Created a `Student` class with attributes for student ID, name, and marks using the constructor.
2. Defined `calculate_total()` method to find total marks using `sum()`.
3. Defined `calculate_percentage()` method to find the percentage based on total marks and number of subjects.
4. Defined `assign_grade()` method to assign a grade based on percentage conditions.
5. Defined `display_details()` method to show all student information.
6. Created an empty list to hold multiple student objects.
7. Took the number of students as input using `input()`.
8. Used a loop to gather student details and marks for multiple subjects.
9. Created a `Student` object and added it to the list.
10. Used a loop to show details of all students.

## **Assignment 2**

1. Created a base class `Employee` with employee ID, name, and basic salary.
2. Created a derived class `SalaryEmployee` using inheritance.
3. Used `super()` to access the parent class constructor.
4. Defined `calculate_hra()` method to find HRA as 20% of basic salary.
5. Defined `calculate_da()` method to find DA as 10% of basic salary.
6. Defined `total_salary()` method to find total salary.
7. Defined `display()` method to show employee details.
8. Took employee details as input from the user.
9. Created an object of the `SalaryEmployee` class.
10. Calculated and displayed HRA, DA, and total salary.

## **Assignment 3**

1. Opened a file `salary.txt` in append mode.
2. Took employee ID, name, and basic salary as input.
3. Calculated total salary by adding basic salary, HRA (20%), and DA (10%).
4. Wrote employee ID, name, and total salary into the file.
5. Closed the file after writing.
6. Opened the file in read mode and showed its contents.
